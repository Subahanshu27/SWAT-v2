# Prompt service — READ ONLY, no DB writes.
# Optional: enables fallback read from workflow_runs (latest successful prompt).
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

PORT=8788
HOST=127.0.0.1

# Baseline trust mode for SWAT preflight (use prompt_changed in prod)
SWAT_BASELINE_MODE=prompt_changed
